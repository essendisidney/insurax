# InsuraX console

Next.js operator, partner, and customer console for the InsuraX insurance operating platform.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Without Supabase env vars the console runs in demo mode (persona login). With `NEXT_PUBLIC_SUPABASE_*` it uses Auth + Postgres.
